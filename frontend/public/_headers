# index.html은 절대 캐시하지 않는다 — 매 배포마다 /assets 해시가 바뀌므로,
# stale index가 죽은 해시를 가리켜 흰화면 나는 것을 막는다.
/
  Cache-Control: no-store
/index.html
  Cache-Control: no-store

# 해시 박힌 정적 에셋은 내용주소(content-hash)라 영구 캐시 안전.
/assets/*
  Cache-Control: public, max-age=31536000, immutable
